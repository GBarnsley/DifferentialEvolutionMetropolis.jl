var documenterSearchIndex = {"docs":
[{"category":"section","location":"hmc/#Combining-HMC-with-differential-evolution","page":"Combining HMC with differential evolution","text":"Hamiltonian Monte Carlo (HMC, and its self-tuning variant NUTS) uses gradients to glide through awkward local geometry, but a single Hamiltonian trajectory will not cross a deep low-density barrier, so a lone HMC chain tends to stay in whichever mode it started in.\n\nThe differential evolution updates in this package have a complementary strength. They move each chain using differences between members of the population, which lets them hop between separated modes cheaply — but only when those modes look alike up to a shift, and in high dimensions their within-mode mixing slows down. HMC is the reverse: with a well-chosen mass matrix it explores a correlated, high-dimensional mode efficiently, but a single trajectory will not cross the gap between separated modes. The hard case for either method alone is therefore a high-dimensional posterior with separated modes of different shape — one shared mass matrix cannot precondition them all at once.\n\nThis is exactly the situation where it pays to combine the two: run an HMC kernel on each chain for gradient-guided local exploration, and a setup_subspace_sampling (DREAM-z) update for population moves. When AdvancedHMC.jl is loaded this package exposes setup_hmc_update, which wraps an AdvancedHMC sampler so it can act as one update inside a setup_sampler_scheme composite, sharing a single, population-pooled metric across all of the chains — or, as we will see, a metric estimated separately for each mode.","title":"Combining HMC with differential evolution"},{"category":"section","location":"hmc/#A-high-dimensional-bimodal-target","page":"Combining HMC with differential evolution","text":"We use a D-dimensional mixture of two Gaussians, separated in location so the population's difference moves can jump between them, but with opposite correlation structure: the + mode is an AR(1) covariance with ρ = +0.9 (neighbouring coordinates positively correlated), the − mode an AR(1) with ρ = −0.9 (neighbouring coordinates anti-correlated). In every coordinate plane the two modes are ellipses tilted in opposite directions, so a single dense mass matrix is a compromise that fits neither — the situation per_cluster_metric is built for. The dimension is what makes this bite: in high D the difference proposals mix slowly within a mode, so the per-mode-preconditioned HMC kernel does the local work.\n\nHMC needs gradients, so the log density must expose them. We write it against the LogDensityProblems interface and wrap it with LogDensityProblemsAD so that ForwardDiff supplies the gradient.\n\nusing DifferentialEvolutionMetropolis, AbstractMCMC\nusing AdvancedHMC, ForwardDiff, LogDensityProblemsAD, LogDensityProblems\nusing Distributions, Random, LinearAlgebra\n\nconst D = 20                          # high enough that within-mode mixing needs HMC\nconst SEP = 4.0                       # the two modes are centred at ±SEP in every coordinate\nconst W_PLUS = 0.6                    # weight of the + mode\n\nar1(ρ) = Symmetric([ρ^abs(i - j) for i in 1:D, j in 1:D])\nconst Σ_PLUS  = ar1(0.9)              # + mode: positively correlated neighbours\nconst Σ_MINUS = ar1(-0.9)            # − mode: anti-correlated neighbours\nconst μ_PLUS  = fill(SEP, D)\nconst μ_MINUS = fill(-SEP, D)\nconst P_PLUS  = inv(Σ_PLUS);  const LD_PLUS  = logdet(Σ_PLUS)\nconst P_MINUS = inv(Σ_MINUS); const LD_MINUS = logdet(Σ_MINUS)\n\nstruct CorrMixture end\nLogDensityProblems.dimension(::CorrMixture) = D\nLogDensityProblems.capabilities(::Type{CorrMixture}) = LogDensityProblems.LogDensityOrder{0}()\nfunction LogDensityProblems.logdensity(::CorrMixture, x)\n    dp = x .- μ_PLUS; dm = x .- μ_MINUS\n    lpP = log(W_PLUS)     - 0.5 * dot(dp, P_PLUS, dp)  - 0.5 * LD_PLUS\n    lpM = log(1 - W_PLUS) - 0.5 * dot(dm, P_MINUS, dm) - 0.5 * LD_MINUS\n    c = max(lpP, lpM)\n    return c + log(exp(lpP - c) + exp(lpM - c))      # mixture of the two Gaussians\nend\n\nmodel = AbstractMCMC.LogDensityModel(ADgradient(:ForwardDiff, CorrMixture()))\n\nThe modes are 2·SEP apart in every coordinate, so difference-based jumps can bridge them, yet their correlations are opposite, so no single mass matrix preconditions both — exactly the regime where giving each mode its own metric pays off.","title":"A high-dimensional bimodal target"},{"category":"section","location":"hmc/#Building-the-HMC-update","page":"Combining HMC with differential evolution","text":"setup_hmc_update takes any AdvancedHMC sampler and decomposes it into the modular pieces (metric, integrator, kernel, adaptor) that the harness drives directly. The simplest way to construct one is the NUTS wrapper. NUTS/HMC/HMCDA store their metric as a symbol and only size it once the model is known, so here you must tell setup_hmc_update the number of parameters via n_dims:\n\nhmc_update = setup_hmc_update(NUTS(0.8); n_dims = D)\nnothing # hide\n\nIf you would rather assemble the kernel yourself, setup_hmc_update also accepts a fully-built HMCSampler. Because that already carries a correctly-sized metric, n_dims is not needed:\n\nmetric = DiagEuclideanMetric(D)\nintegrator = Leapfrog(0.1)\nκ = HMCKernel(Trajectory{MultinomialTS}(integrator, GeneralisedNoUTurn(10, 1000.0)))\nadaptor = StanHMCAdaptor(MassMatrixAdaptor(metric), StepSizeAdaptor(0.8, integrator))\n\ncustom_hmc_update = setup_hmc_update(HMCSampler(κ, metric, adaptor))\nnothing # hide\n\nBoth styles produce the same kind of update; the example below uses the NUTS() form.","title":"Building the HMC update"},{"category":"section","location":"hmc/#Estimating-the-metric-from-the-memory-archive","page":"Combining HMC with differential evolution","text":"warning: Experimental\nThe archive-based metric strategies described here — memory_metric, cluster_pooled_metric, and per_cluster_metric — are experimental; their behaviour and interface may change in future releases, and their effectiveness depends on the target's mode structure (see the example below).\n\nBy default the HMC update tunes its mass matrix the usual AdvancedHMC way: every chain's position is fed into a windowed Welford estimator once per warm-up step, and the mass matrix is committed at the end of each adaptation window (the estimator resetting between windows). That already pools across the whole population, but it only ever sees the current window's draws. Because this package also keeps a historical archive when memory = true, the metric can instead be estimated from that much larger pool. Passing memory_metric as the metric_strategy replaces the windowed adaptor with a covariance computed in one batch over the memory archive (thousands of past positions), recomputed wholesale every every HMC steps rather than accumulated online. Because the archive keeps growing, the metric is recomputed on that cadence throughout both warm-up and sampling, so it never goes stale (changing the mass matrix between trajectories is valid — each HMC step is correct for whatever metric it uses); the step size adapts through dual averaging during warm-up and is then fixed.\n\nBecause it reads the archive, memory_metric requires memory = true and is incompatible with parallel tempering (there the archive interleaves hot-chain positions, so the covariance would mix temperatures); both raise an error on the first warm-up step. Annealing is fine — its chains all cool to the cold target, so the archive ends up a clean cold-target sample.\n\nmem_hmc_update = setup_hmc_update(\n    NUTS(0.8); n_dims = D, metric_strategy = memory_metric()\n)\nnothing # hide\n\nThe estimate takes the shape of the metric the update was built with. With the default NUTS(0.8) metric it is diagonal (per-coordinate variances). Asking for a dense metric instead gives the full covariance, so the metric captures correlations between parameters — the main reason to estimate from a large archive in the first place, since thousands of stored draws make a full D×D covariance well-conditioned where a single chain's warm-up window would not. A dense memory metric is built simply by handing setup_hmc_update a dense AdvancedHMC sampler:\n\ndense_mem_update = setup_hmc_update(\n    NUTS(0.8; metric = :dense); n_dims = D, metric_strategy = memory_metric()\n)\nnothing # hide\n\nThe dense metric is safe under parallel = true: each chain runs its trajectory against a private-scratch copy of the metric, so the shared scratch buffer a DenseEuclideanMetric carries cannot race. Two keywords tune either shape:\n\nshrinkage (λ ∈ [0, 1]) blends the estimate toward an isotropic target (a scaled identity), bounding the metric's condition number. This is worth raising when the population is small or some direction is nearly degenerate (e.g. a strongly correlated parameter pair), where a raw covariance can be noisy, extreme, or — for the dense metric — singular. A small ridge is always added so the metric stays positive-definite.\nevery recomputes the covariance every every HMC steps (in both warm-up and sampling), trading freshness for the cost of the covariance pass.\n\n# A more conservative variant: shrink toward isotropy and recompute every 10 warm-up steps.\nconservative = setup_hmc_update(\n    NUTS(0.8); n_dims = D, metric_strategy = memory_metric(; shrinkage = 0.25, every = 10)\n)\nnothing # hide","title":"Estimating the metric from the memory archive"},{"category":"section","location":"hmc/#Multimodal-targets:-the-cluster-pooled-metric","page":"Combining HMC with differential evolution","text":"The plain memory_metric breaks down when the posterior has well-separated modes: the archive then spans every mode, so its covariance is dominated by the between-mode spread rather than the shape of any individual mode, and the resulting metric fits none of them. cluster_pooled_metric fixes this while still producing a single, position-independent metric. It clusters the archive, subtracts each point's cluster mean, and pools the centred deviations into one covariance — stripping the between-mode spread but keeping the (shared) within-mode shape.\n\ncluster_hmc_update = setup_hmc_update(\n    NUTS(0.8); n_dims = D, metric_strategy = cluster_pooled_metric()\n)\nnothing # hide\n\nIt carries the same requirements as memory_metric (memory = true, no parallel tempering) and takes the same shrinkage/every keywords, plus kmax to cap the cluster count (chosen automatically up to that bound). The adjustment is gated on a cheap multimodality test, so on a unimodal target it costs almost nothing and reduces exactly to memory_metric; it is worth reaching for only when modes are expected to separate.","title":"Multimodal targets: the cluster-pooled metric"},{"category":"section","location":"hmc/#Per-mode-metrics-for-modes-of-different-shape","page":"Combining HMC with differential evolution","text":"cluster_pooled_metric still pools the clusters into one metric, so it can only carry a single shape — fine when the modes differ in location but share a shape, useless when they differ in shape too. per_cluster_metric keeps the cluster covariances separate, one metric per mode, and routes each chain to its current mode's metric (by nearest cluster centre to the chain's position). Each mode then gets its own preconditioner.\n\nper_cluster_hmc_update = setup_hmc_update(\n    NUTS(0.8); n_dims = D, metric_strategy = per_cluster_metric()\n)\nnothing # hide\n\nThe K metrics (and their centres) are recomputed only on the recompute cadence, but each chain is relabelled to its nearest mode's metric before every HMC step, off its current position — so a chain that has wandered into another mode immediately runs under that mode's shape. For well-separated modes this label is constant throughout each basin, so the per-mode metric is the optimal preconditioner and the choice is independent of the step's own trajectory (the setting it is designed for). For close or connected modes, whose trajectories can bridge clusters, picking the metric from the step's own starting position trades a small boundary bias for always matching the chain's current mode. It carries the same requirements and shrinkage/every/kmax keywords as cluster_pooled_metric, and gates to one cluster (= memory_metric) on a unimodal archive.","title":"Per-mode metrics for modes of different shape"},{"category":"section","location":"hmc/#Seeding-the-memory-and-sampling","page":"Combining HMC with differential evolution","text":"These are the z-variant updates: with memory = true the difference vectors come from a historical archive Z, so only a few chains are needed (we use 3). But Z must contain both modes from the start — the default fill adds random points near the origin, which in high D sit far from either mode and make the difference vectors useless. We seed Z with draws from both modes (50/50) by passing them as extra initial_position entries beyond the 3 chains and setting N₀ to keep them.\n\nCrossing two separated modes also needs a full-dimensional jump: a γ = 1 subspace update with cr = 1 moves every coordinate by a full archive difference at once. (A partial-subspace jump moves only some coordinates, landing a chain between the modes where the density is negligible, so it is rejected.)\n\n# 3 starting chains followed by a memory archive seeded 50/50 across the two modes.\nfunction seeded_positions(rng; n_mem = 60)\n    Lp = cholesky(Σ_PLUS).L\n    Lm = cholesky(Σ_MINUS).L\n    plus  = [μ_PLUS  .+ Lp * randn(rng, D) for _ in 1:(n_mem ÷ 2)]\n    minus = [μ_MINUS .+ Lm * randn(rng, D) for _ in 1:(n_mem ÷ 2)]\n    chains = [plus[1], minus[1], plus[2]]               # the 3 chains span both modes\n    return vcat(chains, plus[3:end], minus[2:end])      # the rest seed the memory\nend\n\nscheme = setup_sampler_scheme(\n    setup_hmc_update(NUTS(0.8; metric = :dense); n_dims = D, metric_strategy = per_cluster_metric()),\n    setup_subspace_sampling(),                  # local population moves\n    setup_subspace_sampling(γ = 1.0, cr = 1.0); # full-dimensional mode jump\n    w = [0.5, 0.3, 0.2],\n)\n\nout = sample(\n    Random.Xoshiro(1), model, scheme, 5000;\n    n_chains = 3, num_warmup = 2000,\n    initial_position = seeded_positions(Random.Xoshiro(11)), N₀ = 60,\n    memory = true, progress = false, chain_type = DifferentialEvolutionOutput,\n)\nnothing # hide","title":"Seeding the memory and sampling"},{"category":"section","location":"hmc/#What-it-recovers","page":"Combining HMC with differential evolution","text":"Both modes are represented, and — the payoff of per-mode metrics — each mode's distinct correlation is recovered, even though the two are opposite:\n\nusing Statistics\n\ndraws = reshape(out.samples, :, D)\nin_plus = vec(sum(draws; dims = 2)) .> 0\n\nprintln(\"P(+ mode)            = \", round(mean(in_plus); digits = 2), \"   (true \", W_PLUS, \")\")\nprintln(\"corr(x1, x2)  + mode = \", round(cor(draws[in_plus, 1],  draws[in_plus, 2]);  digits = 2), \"   (true +0.9)\")\nprintln(\"corr(x1, x2)  − mode = \", round(cor(draws[.!in_plus, 1], draws[.!in_plus, 2]); digits = 2), \"   (true −0.9)\")\n\nA scatter of the first two coordinates shows the two modes recovered with their opposite tilts:\n\nusing Plots\n\nscatter(draws[in_plus, 1], draws[in_plus, 2];\n    label = \"+ mode\", ms = 2, alpha = 0.3, color = :steelblue)\nscatter!(draws[.!in_plus, 1], draws[.!in_plus, 2];\n    label = \"− mode\", ms = 2, alpha = 0.3, color = :firebrick)\nplot!(xlabel = \"x₁\", ylabel = \"x₂\", title = \"Two 20-D modes with opposite correlation\")\n\nTwo honest points. First, recovering each mode's shape is where the per-mode metric earns its keep: a single pooled metric would have to compromise between +0.9 and −0.9 and fit neither, whereas per_cluster_metric gives each mode its own dense preconditioner and reproduces both correlations to within a few percent. Second, the mode weight P(+) is the genuinely hard part: even with the memory seeded across both modes and full-dimensional jumps, crossing between two well-separated 20-dimensional modes is rare, so P(+) moves only slowly away from where the chains began. That between-mode mixing is the difficult part of high-dimensional multimodality, and it is largely independent of which metric you choose — the metric governs how efficiently each mode is explored once a chain is in it.\n\nThe broader point: differential-evolution moves and HMC are complementary — the population's difference proposals jump between separated modes, while HMC, with a per-mode metric, explores each high-dimensional mode efficiently. The archive-based metric strategies are an experimental lever for that second part: memory_metric for a single mode (or several alike up to a shift), cluster_pooled_metric for modes that differ in location but share a shape, and per_cluster_metric for modes that differ in shape, as here. As always, benchmark on your own posterior.","title":"What it recovers"},{"category":"section","location":"custom/#Customizing-your-sampler","page":"Customizing your sampler","text":"This document describes how to extend DifferentialEvolutionMetropolis.jl with your own custom components. You can define custom stopping criteria, diagnostic checks, and proposal distributions (updates).","title":"Customizing your sampler"},{"category":"section","location":"custom/#Custom-Stopping-Criteria","page":"Customizing your sampler","text":"To create a custom stopping criterion, you need to define a function that follows the AbstractMCMC interface, similar to r̂_stopping_criteria which is available if MCMCDiagnosticTools.jl is loaded. The function will be called during sampling to determine when to stop. See the AbstractMCMC documentation for details.\n\nThe stopping criterion function has the following signature:\n\nfunction your_stopping_criteria(\n    rng::AbstractRNG,\n    model::AbstractModel, \n    sampler::AbstractDifferentialEvolutionSampler,\n    samples::Vector{DifferentialEvolutionSample},\n    state::DifferentialEvolutionState,\n    iteration::Int;\n    kwargs...\n)\n\nrng: Random number generator (required by AbstractMCMC interface, usually unused)\nmodel: The model being sampled (required by interface, usually unused)  \nsampler: The differential evolution sampler (required by interface, usually unused)\nsamples: Vector of all collected samples from all chains\nstate: Current sampler state (required by interface, usually unused)\niteration: Current iteration number\nkwargs...: Additional keyword arguments passed via AbstractMCMC.sample\n\nThe function should return true if sampling should stop, and false otherwise.\n\nHere is an example of a very simple stopping criterion that stops sampling after a maximum number of iterations has been reached:\n\nusing DifferentialEvolutionMetropolis, AbstractMCMC\n\nfunction max_iterations_stopping(\n    rng::AbstractRNG,\n    model::AbstractModel,\n    sampler::AbstractDifferentialEvolutionSampler, \n    samples::Vector{DifferentialEvolutionSample{V, VV}},\n    state::DifferentialEvolutionState{T, V, VV, A},\n    iteration::Int;\n    max_iterations::Int = 10000,\n    kwargs...\n) where {T<:Real, V<:AbstractVector{T}, VV<:AbstractVector{V}, A<:AbstractDifferentialEvolutionAdaptiveState{T}}\n    if iteration >= max_iterations\n        println(\"Reached maximum iterations ($max_iterations), stopping.\")\n        return true\n    end\n    return false\nend\n\n# Usage with AbstractMCMC.sample\nusing LogDensityProblems\n\nmodel = LogDensityModel(your_log_density)\nsampler = setup_de_update()\n\nresult = sample(\n    rng, \n    model, \n    sampler, \n    max_iterations_stopping;\n    max_iterations = 5000,  # passed as keyword argument\n    n_chains = 4\n)","title":"Custom Stopping Criteria"},{"category":"section","location":"custom/#Custom-Proposal-Distributions","page":"Customizing your sampler","text":"You can create your own proposal distributions by defining a new sampler type that subtypes AbstractDifferentialEvolutionSampler and implementing the proposal method.\n\nThe method signature for the proposal is:\n\nfunction proposal!(\n    state::DifferentialEvolutionMetropolis.DifferentialEvolutionState, \n    sampler::YourSampler, \n    current_state::Int\n)\n\nstate: The current state containing all chain positions, log-densities, and chain specific rngs\nsampler: An instance of your custom sampler struct\ncurrent_state: The index of the chain to be updated\n\nThe function should modify state.xₚ[current_state] = proposed_position and return a named tuple with at least (offset = hastings_correction) where:\n\nproposed_position: The proposed new position (vector)\noffset: Hastings ratio correction in log-space (typically 0.0 for symmetric proposals)","title":"Custom Proposal Distributions"},{"category":"section","location":"custom/#Chains-Required-for-Memoryless-Sampling","page":"Customizing your sampler","text":"When memoryless sampling is used (memory = false), the sampler validates that the number of parallel chains is sufficient for the sampler's proposals (since they cannot be drawn from the memory database). By default, any custom sampler subtyping AbstractDifferentialEvolutionSampler has a conservative fallback requiring at least 3 chains. If your custom sampler has a different requirement, you should implement the chains_required(sampler) trait function. For example:\n\n# If your sampler only needs 1 chain (e.g., standard random walk):\nDifferentialEvolutionMetropolis.chains_required(::YourSampler) = 1\n\nHere is an example of a simple Metropolis-Hastings random walk update with a fixed step size:\n\nusing DifferentialEvolutionMetropolis, Distributions, Random\n\n# Define the struct for the sampler\nstruct MetropolisHastingsUpdate <: DifferentialEvolutionMetropolis.AbstractDifferentialEvolutionSampler\n    proposal_distribution::MvNormal\nend\n\n# Implement the proposal function\nfunction DifferentialEvolutionMetropolis.proposal!(\n    state::DifferentialEvolutionMetropolis.DifferentialEvolutionState,\n    sampler::MetropolisHastingsUpdate,\n    current_state::Int\n)\n    # Get the current position of this chain\n    x_current = state.x[current_state]\n    \n    # Propose a new point (stored in state) using a random walk\n    state.xₚ[current_state] .= x_current .+ rand(state.rngs[current_state], sampler.proposal_distribution)\n    \n    # The proposal is symmetric, so no Hastings correction needed\n    return (offset = 0.0)\nend\n\nDifferentialEvolutionMetropolis.chains_required(::MetropolisHastingsUpdate) = 1\n","title":"Chains Required for Memoryless Sampling"},{"category":"section","location":"custom/#Adaptive-Proposals-with-step_warmup","page":"Customizing your sampler","text":"For proposals that require adaptation during warm-up, you need to implement the step_warmup as well. This is called during the warm-up phase. Unless you want your sampler to be always adaptive then you must implement step.\n\nYou'll also need to define adaptive state structures and methods. Here's an example of an adaptive Metropolis-Hastings sampler:\n\nusing AbstractMCMC, DifferentialEvolutionMetropolis, LinearAlgebra, Statistics\n# Define adaptive state\nmutable struct AdaptiveMetropolisState{T<:Real} <:DifferentialEvolutionMetropolis.AbstractDifferentialEvolutionAdaptiveState{T}\n    proposal_cov::Matrix{T}\n    adaptation_count::Int\n    running_mean::Vector{T}\n    running_cov::Matrix{T}\nend\n\n# Define the adaptive sampler  \nstruct AdaptiveMetropolisUpdate{T<:Real} <: DifferentialEvolutionMetropolis.AbstractDifferentialEvolutionSampler\n    initial_cov::Matrix{T}\n    adapt_after::Int\n    adapt_every::Int\n    adapt_scale::T\nend\n\n# Constructor\nfunction AdaptiveMetropolisUpdate(\n    n_params::Int;\n    initial_std::Float64 = 0.1,\n    adapt_after::Int = 200,\n    adapt_every::Int = 100,\n    adapt_scale::Float64 = 2.38^2\n)\n    initial_cov = (initial_std^2) * I(n_params)\n    return AdaptiveMetropolisUpdate{Float64}(initial_cov, adapt_after, adapt_every, adapt_scale / n_params)\nend\n\n# Initialize adaptive state\nfunction DifferentialEvolutionMetropolis.initialize_adaptive_state(sampler::AdaptiveMetropolisUpdate{T}, model_wrapper::AbstractMCMC.LogDensityModel, n_chains::Int) where {T}\n    n_params = dimension(model_wrapper.logdensity)\n    return AdaptiveMetropolisState{T}(\n        copy(sampler.initial_cov),\n        0,\n        zeros(T, n_params),\n        copy(sampler.initial_cov)\n    )\nend\n\n# Fix sampler (convert adaptive to non-adaptive)\nfunction DifferentialEvolutionMetropolis.fix_sampler(sampler::AdaptiveMetropolisUpdate{T}, adaptive_state::AdaptiveMetropolisState{T}) where {T}\n    return MetropolisHastingsUpdate(MvNormal(zeros(T, size(adaptive_state.proposal_cov, 1)), adaptive_state.proposal_cov))\nend\n\n# Proposal method (same as non-adaptive)\nfunction DifferentialEvolutionMetropolis.proposal!(\n    state::DifferentialEvolutionMetropolis.DifferentialEvolutionState,\n    sampler::AdaptiveMetropolisUpdate,\n    current_state::Int\n)\n    x_current = state.x[current_state]\n    # Use current proposal covariance from adaptive state\n    state.xₚ[current_state] .= rand(state.rngs[current_state], MvNormal(x_current, state.adaptive_state.proposal_cov))\n    return (offset = 0.0)\nend\n\nDifferentialEvolutionMetropolis.chains_required(::AdaptiveMetropolisUpdate) = 1\n\n\n# Adaptive step during warm-up\nfunction step_warmup(\n    rng::AbstractRNG,\n    model_wrapper::AbstractMCMC.LogDensityModel,\n    sampler::AdaptiveMetropolisUpdate{T},\n    state::DifferentialEvolutionMetropolis.DifferentialEvolutionState{T, <:AdaptiveMetropolisState{T}};\n    parallel::Bool = false,\n    kwargs...\n) where {T<:Real}\n    \n    # Perform regular step\n    sample, new_state = step(rng, model_wrapper, sampler, state; parallel = parallel, kwargs...)\n    \n    # Update adaptive parameters in-place\n    adapt_state = new_state.adaptive_state\n    adapt_state.adaptation_count += 1\n    \n    # Only adapt after burn-in period and at specified intervals\n    if adapt_state.adaptation_count > sampler.adapt_after && adapt_state.adaptation_count % sampler.adapt_every == 0\n        \n        # Compute empirical covariance from current chain positions\n        positions = reduce(hcat, new_state.x)'  # Convert to matrix\n        empirical_cov = cov(positions)\n        \n        # Update proposal covariance with regularization in-place\n        adapt_state.proposal_cov .= sampler.adapt_scale * empirical_cov + 1e-6 * I\n    end\n    \n    return sample, new_state\nend","title":"Adaptive Proposals with step_warmup"},{"category":"section","location":"custom/#Example:-Using-Custom-Components","page":"Customizing your sampler","text":"Here is a complete example that shows how to use custom components with the new AbstractMCMC interface:\n\nusing Distributions, TransformedLogDensities, LinearAlgebra, TransformVariables, Plots\n\n# Set up a simple log-density to sample from (a 2D standard normal distribution)\n\nld = TransformedLogDensity(as(Array, 2), x -> -sum(x.^2) / 2)\ndimension(ld) = 2\nmodel = AbstractMCMC.LogDensityModel(ld)\n\n# Create custom samplers\nsimple_mh = MetropolisHastingsUpdate(MvNormal([0.0, 0.0], 0.1 * I))\nadaptive_mh = AdaptiveMetropolisUpdate(2; initial_std = 0.1)\n\n# Create a composite sampler scheme\nmy_sampler_scheme = setup_sampler_scheme(\n    simple_mh, \n    adaptive_mh;\n    w = [0.3, 0.7]  # Use adaptive sampler 70% of the time\n)\n\n# Sample using AbstractMCMC.sample \nresult = sample(\n    Random.default_rng(),\n    model,\n    my_sampler_scheme,\n    5000;\n    n_chains = 6,\n    num_warmup = 10000, #adaptive steps\n    memory = true,\n    parallel = false,\n    chain_type = DifferentialEvolutionOutput\n)\n\nplot(result.samples[:, :, 1])\nplot(result.samples[:, :, 2])","title":"Example: Using Custom Components"},{"category":"section","location":"#DifferentialEvolutionMetropolis-Documentation","page":"DifferentialEvolutionMetropolis Documentation","text":"Tools for sampling from log-densities using differential evolution algorithms.\n\nSee Sampling from multimodal distributions and Customizing your sampler to get started.\n\nThis package is built upon AbstractMCMC.jl so log-densities should be constructed using that package, and can be used with TransformVariables.jl or Bijectors.jl to control the parameter space.\n\nThe other key dependency is Distributions.jl. Almost every parameter in proposals given here are defined via customizable univariate distributions. Values that are fixed are specified via a Dirac distribution, though in the API these can be specified with any real value. As a warning there are some checks on the given distributions, but in the interest of flexibility it is up to the user to ensure that they are suitable for the given parameter. You can disable any checking of your provided distributions with ; check_args = false if you really want to ruin your sampler efficiency. Distributions can optionally be used to define your log-density, as in the examples given here. \n\nAs far as I am aware, there is one other package that implements differential evolution MCMC in Julia, DifferentialEvolutionMCMC.jl. I opted to implement my own version as I wanted a more flexible API and the subsampling scheme from DREAM. That's not to discredit DifferentialEvolutionMCMC.jl, it has many features this package does not, such as being able to work on optimization problems and parameter blocking.","title":"DifferentialEvolutionMetropolis Documentation"},{"category":"section","location":"#Main-features","page":"DifferentialEvolutionMetropolis Documentation","text":"Original differential evolution, snooker, and adaptive subspace sampling (i.e. from DREAM) updates\nOptional parallel tempering (no swaps yet, information is shared by the DE updates!) and annealing\nComposite samplers, can combine any of the implemented updates (in future I'll wrap other abstractMCMC based samplers)\nEasy to implement your own updates!\nCan output in MCMCChains format, though you use multiple sampling chains (i.e. chains of the DE-chains) these will all be appended together","title":"Main features"},{"category":"section","location":"#Next-Steps","page":"DifferentialEvolutionMetropolis Documentation","text":"A few plans for this package, feel free to suggest features or improvements via issues:\n\nImplement multi-try and delayed rejection DREAM, I avoided these so far since I have been using these samplers for costly log-densities with relatively few parameters, such as one that solve an ODE.\nAdditional diagnostic checks and adaptive schemes.","title":"Next Steps"},{"category":"section","location":"#Contents","page":"DifferentialEvolutionMetropolis Documentation","text":"","title":"Contents"},{"category":"section","location":"#Functions","page":"DifferentialEvolutionMetropolis Documentation","text":"","title":"Functions"},{"category":"section","location":"#Implemented-Sampling-Schemes","page":"DifferentialEvolutionMetropolis Documentation","text":"","title":"Implemented Sampling Schemes"},{"category":"section","location":"#Setup-Functions","page":"DifferentialEvolutionMetropolis Documentation","text":"","title":"Setup Functions"},{"category":"section","location":"#HMC-Setup-Functions","page":"DifferentialEvolutionMetropolis Documentation","text":"","title":"HMC Setup Functions"},{"category":"section","location":"#Core-Sampling-Functions","page":"DifferentialEvolutionMetropolis Documentation","text":"","title":"Core Sampling Functions"},{"category":"section","location":"#Convergence-and-Stopping-Criteria","page":"DifferentialEvolutionMetropolis Documentation","text":"","title":"Convergence and Stopping Criteria"},{"category":"section","location":"#Output","page":"DifferentialEvolutionMetropolis Documentation","text":"The output format can be modified with chain_type, the supported options are Chains from MCMCChains, VNChain from FlexiChains, Any which returns the basic DifferentialEvolutionMetropolis.DifferentialEvolutionSample, and the default option DifferentialEvolutionOutput. If save_final_state = true the format will be (sample::requested format, final_state). If run in parallel using step(model, sampler, parallel_option, n_its, n_meta_chains; n_chains = n_chains) the meta chains and DE chains will be merged into one dimension for both Chains and DifferentialEvolutionOutput, if the final state is saved it will be a vector of length n_meta_chains containing the final state for each.\n\nNote that support for FlexiChains is a bit underutilized as the all samplers currently require all of your parameters to have one type.","title":"Output"},{"category":"section","location":"#Index","page":"DifferentialEvolutionMetropolis Documentation","text":"","title":"Index"},{"category":"function","location":"#DifferentialEvolutionMetropolis.deMC","page":"DifferentialEvolutionMetropolis Documentation","text":"deMC(model_wrapper, n_its; kwargs...)\n\nRun the Differential Evolution Markov Chain (DE-MC) sampler proposed by ter Braak (2006).\n\nThis sampler uses differential evolution updates with optional switching between two scaling factors (γ₁ and γ₂) to enable mode switching.\n\nThis implementation varies slightly from the original: updates within a population occur based on the previous positions to enable easy parallelization.\n\nSee doi.org/10.1007/s11222-006-8769-1 for more information.\n\nThe algorithm runs for a fixed number of iterations with optional burn-in.\n\nArguments\n\nmodel_wrapper: LogDensityModel containing the target log-density function\nn_its: Number of sampling iterations per chain\n\nKeyword Arguments\n\nrng: Random number generator. Defaults to default_rng().\nn_burnin: Number of burn-in iterations. Defaults to n_its * 5.\nsave_burnt: Save burn-in samples in output. Defaults to false.\nγ₁: Primary scaling factor. Defaults to 2.38 / sqrt(2 * dim).\nγ₂: Secondary scaling factor for mode switching. Defaults to 1.0.\np_γ₂: Probability of using γ₂. Defaults to 0.1.\nchain_type: Type of chain to return (e.g., Any, DifferentialEvolutionOutput, MCMCChains.Chains, or FlexiChains.VNChain). Defaults to DifferentialEvolutionOutput.\nsave_final_state: Whether to return the final state along with samples, if true the output will be (samples::chaintype, finalstate). Defaults to false.\n\nGeneric DE Arguments\n\nn_chains: Number of parallel chains. Defaults to max(2 * dimension, 3) for adequate mixing.\nadapt: Whether to enable adaptive behavior during warm-up (if the sampler supports it). Defaults to true.\ninitial_position: Starting positions for chains. Can be nothing (random initialization), or a vector of parameter vectors. If the provided vector is smaller than n_chains + n_hot_chains, it will be expanded; if larger and memory=true, excess positions become initial memory. Defaults to nothing.\nparallel: Whether to evaluate initial log-densities in parallel. Useful for expensive models. Defaults to false.\nn_preallocated_indices: This package provides fast sampling-without-replacement by pre-allocating indices, defaults to 3 (which the most asked for by the implemented samplers). Consider increasing it if you implement your own proposal that calls pick_chains with n_chains > 3.\nsilent: Suppress informational logging during initialization (e.g., initial position adjustments and   memory setup) when true. Defaults to false.\n\nMemory-based Sampling Arguments\n\nmemory: Whether to use memory-based sampling that stores past positions. Memory-based samplers can be more efficient for high-dimensional problems. Defaults to false.\nN₀: Initial memory size for memory-based samplers. Should be ≥ n_chains + n_hot_chains. Defaults to 2 * (n_chains + n_hot_chains).\nupdate_memory: Whether to update the memory with new positions (for memory-based samplers). Defaults to true. Overwrites memory options given at initialization, generally should only be of use if calling step directly.\nmemory_refill: Whether to refill memory when full instead of extending the memory, will replace from the start. Defaults to false.\nmemory_size: Maximum number of positions preallocated per chain in memory. The effective number stored positions is memory_size * (n_chains + n_hot_chains). Defaults to 1001 or 2*num_warmup if that is provided here or via sample. If memory_refill = true this is the maximum number stored before refilling, if  memory_refill = false once the memory is full, the array is extended by another memory_size worth of positions. Set with consideration of available RAM and expected run length.\nmemory_thin_interval: Thinning interval for memory updates. If > 0, only every memory_thin_interval-th position is stored in memory.\n\nParallel Tempering and Simulated Annealing Arguments\n\nn_hot_chains: Number of hot chains for parallel tempering. Defaults to 0 (no parallel tempering).\nmax_temp_pt: Maximum temperature for parallel tempering. Defaults to 2*sqrt(dimension).\nmax_temp_sa: Maximum temperature for simulated annealing. Defaults to max_temp_pt.\nα: Temperature ladder spacing parameter. Controls the geometric spacing between temperatures. Defaults to 1.0.\nannealing: Whether to use simulated annealing (temperature decreases over time). Defaults to false.\nannealing_steps: Number of annealing steps. Defaults to annealing ? num_warmup : 0.\ntemperature_ladder: Pre-defined temperature ladder as a vector of vectors. If provided, overrides automatic temperature ladder creation. Defaults to create_temperature_ladder(n_chains, n_hot_chains, α, max_temp_pt, max_temp_sa, annealing_steps).\n\nkwargs...: Additional keyword arguments passed to AbstractMCMC.sample (e.g., memory_refill, memory_thin_interval, silent). See AbstractMCMC documentation.\n\nReturns\n\ndepends on chain_type, and save_final_state\n\nExample\n\nusing DifferentialEvolutionMetropolis, Random, Distributions\n\n# Define a simple log-density function\nmodel_wrapper(θ) = logpdf(MvNormal([0.0, 0.0], I), θ)\n\n# Run differential evolution MCMC\nresult = deMC(model_wrapper, 1000; n_chains = 10, parallel = false)\n\nNotes\n\nFor non-memory samplers, n_chains should typically be ≥ dimension for good mixing\nMemory-based samplers can work effectively with fewer chains than the problem dimension\nInitial log-densities are computed automatically for all starting positions\nWhen using parallel tempering (n_hot_chains > 0), only the cold chains (first n_chains) are returned in the sample, but all chains participate in the sampling process\nMemory-based samplers with parallel tempering will issue warnings since hot chains typically aren't necessary when using memory\n\nSee also deMCzs, DREAMz, setup_de_update.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.deMC"},{"category":"function","location":"#DifferentialEvolutionMetropolis.deMCzs","page":"DifferentialEvolutionMetropolis Documentation","text":"deMCzs(model_wrapper, n_its; kwargs...)\n\nRun the Differential Evolution Markov Chain with snooker update and historic sampling (DE-MCzs) sampler.\n\nIt combines DE updates with optional snooker moves and uses memory-based sampling to efficiently handle high-dimensional problems with fewer chains.\n\nProposed by ter Braak and Vrugt (2008), see doi.org/10.1007/s11222-008-9104-9.\n\nThe algorithm runs for a fixed number of iterations with optional burn-in.\n\nArguments\n\nmodel_wrapper: LogDensityModel containing the target log-density function\nn_its: Number of sampling iterations per chain\n\nKeyword Arguments\n\nrng: Random number generator. Defaults to default_rng().\nn_burnin: Number of burn-in iterations. Defaults to n_its * 5.\nγ: Scaling factor for DE updates. Defaults to 2.38 / sqrt(2 * dim).\nγₛ: Scaling factor for snooker updates. Defaults to 2.38 / sqrt(2).\np_snooker: Probability of snooker moves. Defaults to 0.1.\nβ: Noise distribution for DE updates. Defaults to Uniform(-1e-4, 1e-4).\nchain_type: Type of chain to return (e.g., Any, DifferentialEvolutionOutput, MCMCChains.Chains, or FlexiChains.VNChain). Defaults to DifferentialEvolutionOutput.\nsave_final_state: Whether to return the final state along with samples, if true the output will be (samples::chaintype, finalstate). Defaults to false.\n\nGeneric DE Arguments\n\nn_chains: Number of parallel chains. Defaults to max(2 * dimension, 3) for adequate mixing.\nadapt: Whether to enable adaptive behavior during warm-up (if the sampler supports it). Defaults to true.\ninitial_position: Starting positions for chains. Can be nothing (random initialization), or a vector of parameter vectors. If the provided vector is smaller than n_chains + n_hot_chains, it will be expanded; if larger and memory=true, excess positions become initial memory. Defaults to nothing.\nparallel: Whether to evaluate initial log-densities in parallel. Useful for expensive models. Defaults to false.\nn_preallocated_indices: This package provides fast sampling-without-replacement by pre-allocating indices, defaults to 3 (which the most asked for by the implemented samplers). Consider increasing it if you implement your own proposal that calls pick_chains with n_chains > 3.\nsilent: Suppress informational logging during initialization (e.g., initial position adjustments and   memory setup) when true. Defaults to false.\n\nMemory-based Sampling Arguments\n\nmemory: Whether to use memory-based sampling that stores past positions. Memory-based samplers can be more efficient for high-dimensional problems. Defaults to true.\nN₀: Initial memory size for memory-based samplers. Should be ≥ n_chains + n_hot_chains. Defaults to 2 * (n_chains + n_hot_chains).\nupdate_memory: Whether to update the memory with new positions (for memory-based samplers). Defaults to true. Overwrites memory options given at initialization, generally should only be of use if calling step directly.\nmemory_refill: Whether to refill memory when full instead of extending the memory, will replace from the start. Defaults to false.\nmemory_size: Maximum number of positions preallocated per chain in memory. The effective number stored positions is memory_size * (n_chains + n_hot_chains). Defaults to 1001 or 2*num_warmup if that is provided here or via sample. If memory_refill = true this is the maximum number stored before refilling, if  memory_refill = false once the memory is full, the array is extended by another memory_size worth of positions. Set with consideration of available RAM and expected run length.\nmemory_thin_interval: Thinning interval for memory updates. If > 0, only every memory_thin_interval-th position is stored in memory.\n\nParallel Tempering and Simulated Annealing Arguments\n\nn_hot_chains: Number of hot chains for parallel tempering. Defaults to 0 (no parallel tempering).\nmax_temp_pt: Maximum temperature for parallel tempering. Defaults to 2*sqrt(dimension).\nmax_temp_sa: Maximum temperature for simulated annealing. Defaults to max_temp_pt.\nα: Temperature ladder spacing parameter. Controls the geometric spacing between temperatures. Defaults to 1.0.\nannealing: Whether to use simulated annealing (temperature decreases over time). Defaults to false.\nannealing_steps: Number of annealing steps. Defaults to annealing ? num_warmup : 0.\ntemperature_ladder: Pre-defined temperature ladder as a vector of vectors. If provided, overrides automatic temperature ladder creation. Defaults to create_temperature_ladder(n_chains, n_hot_chains, α, max_temp_pt, max_temp_sa, annealing_steps).\n\nkwargs...: Additional keyword arguments passed to AbstractMCMC.sample (e.g., memory_refill, memory_thin_interval, silent). See AbstractMCMC documentation.\n\nReturns\n\ndepends on chain_type, and save_final_state\n\nExample\n\nusing DifferentialEvolutionMetropolis, Random, Distributions\n\n# Define a simple log-density function\nmodel_wrapper(θ) = logpdf(MvNormal([0.0, 0.0], I), θ)\n\n# Run differential evolution MCMC\nresult = deMCzs(model_wrapper, 1000; n_chains = 3)\n\nNotes\n\nFor non-memory samplers, n_chains should typically be ≥ dimension for good mixing\nMemory-based samplers can work effectively with fewer chains than the problem dimension\nInitial log-densities are computed automatically for all starting positions\nWhen using parallel tempering (n_hot_chains > 0), only the cold chains (first n_chains) are returned in the sample, but all chains participate in the sampling process\nMemory-based samplers with parallel tempering will issue warnings since hot chains typically aren't necessary when using memory\n\nSee also deMC, DREAMz.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.deMCzs"},{"category":"function","location":"#DifferentialEvolutionMetropolis.DREAMz","page":"DifferentialEvolutionMetropolis Documentation","text":"DREAMz(model_wrapper, n_its; kwargs...)\n\nRun the Differential Evolution Adaptive Metropolis (DREAMz) sampler.\n\nThis advanced adaptive sampler uses subspace sampling with adaptive crossover probabilities. It can switch between scaling factors and includes outlier chain detection/replacement. The algorithm adapts during warm-up and can use memory-based sampling for efficiency.\n\nBased on Vrugt et al. (2009), see doi.org/10.1515/IJNSNS.2009.10.3.273.\n\nThe algorithm runs for a fixed number of iterations with optional burn-in.\n\nArguments\n\nmodel_wrapper: LogDensityModel containing the target log-density function\nn_its: Number of sampling iterations per chain\n\nKeyword Arguments\n\nrng: Random number generator. Defaults to default_rng().\nn_burnin: Number of burn-in iterations. Defaults to n_its * 5.\nγ₁: Primary scaling factor for subspace updates. Defaults to adaptive.\nγ₂: Secondary scaling factor. Defaults to 1.0.\np_γ₂: Probability of using γ₂. Defaults to 0.2.\nn_cr: Number of crossover probabilities for adaptation. Defaults to 3.\ncr₁: Crossover probability for γ₁. Defaults to adaptive.\ncr₂: Crossover probability for γ₂. Defaults to adaptive.\nϵ: Additive noise distribution. Defaults to Uniform(-1e-4, 1e-4).\ne: Multiplicative noise distribution. Defaults to Normal(0.0, 1e-2).\nchain_type: Type of chain to return (e.g., Any, DifferentialEvolutionOutput, MCMCChains.Chains, or FlexiChains.VNChain). Defaults to DifferentialEvolutionOutput.\nsave_final_state: Whether to return the final state along with samples, if true the output will be (samples::chaintype, finalstate). Defaults to false.\n\nGeneric DE Arguments\n\nn_chains: Number of parallel chains. Defaults to max(2 * dimension, 3) for adequate mixing.\nadapt: Whether to enable adaptive behavior during warm-up (if the sampler supports it). Defaults to true.\ninitial_position: Starting positions for chains. Can be nothing (random initialization), or a vector of parameter vectors. If the provided vector is smaller than n_chains + n_hot_chains, it will be expanded; if larger and memory=true, excess positions become initial memory. Defaults to nothing.\nparallel: Whether to evaluate initial log-densities in parallel. Useful for expensive models. Defaults to false.\nn_preallocated_indices: This package provides fast sampling-without-replacement by pre-allocating indices, defaults to 3 (which the most asked for by the implemented samplers). Consider increasing it if you implement your own proposal that calls pick_chains with n_chains > 3.\nsilent: Suppress informational logging during initialization (e.g., initial position adjustments and   memory setup) when true. Defaults to false.\n\nMemory-based Sampling Arguments\n\nmemory: Whether to use memory-based sampling that stores past positions. Memory-based samplers can be more efficient for high-dimensional problems. Defaults to true.\nN₀: Initial memory size for memory-based samplers. Should be ≥ n_chains + n_hot_chains. Defaults to 2 * (n_chains + n_hot_chains).\nupdate_memory: Whether to update the memory with new positions (for memory-based samplers). Defaults to true. Overwrites memory options given at initialization, generally should only be of use if calling step directly.\nmemory_refill: Whether to refill memory when full instead of extending the memory, will replace from the start. Defaults to false.\nmemory_size: Maximum number of positions preallocated per chain in memory. The effective number stored positions is memory_size * (n_chains + n_hot_chains). Defaults to 1001 or 2*num_warmup if that is provided here or via sample. If memory_refill = true this is the maximum number stored before refilling, if  memory_refill = false once the memory is full, the array is extended by another memory_size worth of positions. Set with consideration of available RAM and expected run length.\nmemory_thin_interval: Thinning interval for memory updates. If > 0, only every memory_thin_interval-th position is stored in memory.\n\nParallel Tempering and Simulated Annealing Arguments\n\nn_hot_chains: Number of hot chains for parallel tempering. Defaults to 0 (no parallel tempering).\nmax_temp_pt: Maximum temperature for parallel tempering. Defaults to 2*sqrt(dimension).\nmax_temp_sa: Maximum temperature for simulated annealing. Defaults to max_temp_pt.\nα: Temperature ladder spacing parameter. Controls the geometric spacing between temperatures. Defaults to 1.0.\nannealing: Whether to use simulated annealing (temperature decreases over time). Defaults to false.\nannealing_steps: Number of annealing steps. Defaults to annealing ? num_warmup : 0.\ntemperature_ladder: Pre-defined temperature ladder as a vector of vectors. If provided, overrides automatic temperature ladder creation. Defaults to create_temperature_ladder(n_chains, n_hot_chains, α, max_temp_pt, max_temp_sa, annealing_steps).\n\nkwargs...: Additional keyword arguments passed to AbstractMCMC.sample (e.g., memory_refill, memory_thin_interval, silent). See AbstractMCMC documentation.\n\nReturns\n\ndepends on chain_type, and save_final_state\n\nExample\n\nusing DifferentialEvolutionMetropolis, Random, Distributions\n\n# Define a simple log-density function\nmodel_wrapper(θ) = logpdf(MvNormal([0.0, 0.0], I), θ)\n\n# Run DREAM with subspace sampling\nresult = DREAMz(model_wrapper, 1000; n_chains = 10, memory = false)\n\nNotes\n\nFor non-memory samplers, n_chains should typically be ≥ dimension for good mixing\nMemory-based samplers can work effectively with fewer chains than the problem dimension\nInitial log-densities are computed automatically for all starting positions\nWhen using parallel tempering (n_hot_chains > 0), only the cold chains (first n_chains) are returned in the sample, but all chains participate in the sampling process\nMemory-based samplers with parallel tempering will issue warnings since hot chains typically aren't necessary when using memory\n\nSee also deMC, deMCzs, setup_subspace_sampling.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.DREAMz"},{"category":"function","location":"#DifferentialEvolutionMetropolis.setup_sampler_scheme","page":"DifferentialEvolutionMetropolis Documentation","text":"Create a composite sampler scheme from multiple differential evolution update steps.\n\nThe update method used in each iteration for each chain is randomly selected from the provided update steps according to their weights. This allows combining different sampling strategies (e.g., DE updates with snooker updates) in a single sampler.\n\nArguments\n\nupdates...: One or more differential evolution sampler objects created by functions like setup_de_update, setup_snooker_update, setup_subspace_sampling.\n\nKeyword Arguments\n\nw: Vector of weights for each update step. If not provided, all updates are chosen with equal probability. Weights must be non-negative and will be automatically normalized.\n\nReturns\n\nA DifferentialEvolutionCompositeSampler that can be used with AbstractMCMC.sample.\n\nExamples\n\nusing DifferentialEvolutionMetropolis\n\n# Only snooker updates\nsampler1 = setup_sampler_scheme(setup_snooker_update())\n\n# DE and Snooker with equal probability\nsampler2 = setup_sampler_scheme(setup_de_update(), setup_snooker_update())\n\n# Snooker 10% of the time, DE 90% of the time\nsampler3 = setup_sampler_scheme(setup_de_update(), setup_snooker_update(); w = [0.9, 0.1])\n\nSee also setup_de_update, setup_snooker_update, setup_subspace_sampling.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.setup_sampler_scheme"},{"category":"function","location":"#DifferentialEvolutionMetropolis.setup_de_update","page":"DifferentialEvolutionMetropolis Documentation","text":"Set up a Differential Evolution (DE) update step for MCMC sampling.\n\nCreates a sampler that proposes new states by adding scaled difference vectors between randomly selected chains plus small noise. This is the core update mechanism from the original DE-MC algorithm by ter Braak (2006).\n\nSee doi.org/10.1007/s11222-006-8769-1 for more information.\n\nKeyword Arguments\n\nγ: Scaling factor for the difference vector. Can be a Real (fixed value), a UnivariateDistribution (random scaling), or nothing (automatic based on n_dims). Defaults to nothing.\nβ: Distribution for small noise added to proposals. Must be a univariate continuous distribution. Defaults to Uniform(-1e-4, 1e-4).\nn_dims: Problem dimension used for automatic γ selection. If > 0 and γ is nothing, sets γ to the theoretically optimal 2.38 / sqrt(2 * n_dims). If ≤ 0, uses Uniform(0.8, 1.2). Defaults to 0.\ncheck_args: Whether to validate input distributions. Defaults to true.\n\nReturns\n\nA DifferentialEvolutionSampler that can be used with setup_sampler_scheme or step or sample from AbstractMCMC.\n\nExample\n\nusing DifferentialEvolutionMetropolis, Distributions\n\n# Setup differential evolution update with custom parameters\nde_update = setup_de_update(γ = 1.0, β = Normal(0.0, 0.01))\n\nSee also setup_snooker_update, setup_subspace_sampling, setup_sampler_scheme.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.setup_de_update"},{"category":"function","location":"#DifferentialEvolutionMetropolis.setup_snooker_update","page":"DifferentialEvolutionMetropolis Documentation","text":"Set up a Snooker update step for MCMC sampling.\n\nCreates a sampler that proposes moves along the line connecting the current position to a projection point, scaled by the difference between two other randomly selected chains. This update can help with sampling from distributions with complex geometries by making larger moves in effective directions.\n\nSee doi.org/10.1007/s11222-008-9104-9 for more information.\n\nKeyword Arguments\n\nγ: Scaling factor for the projection. Can be a Real (fixed value), a UnivariateDistribution (random scaling), or nothing (automatic based on deterministic_γ). Defaults to nothing.\ndeterministic_γ: When γ is nothing, determines the automatic value. If true, uses the theoretically optimal 2.38 / sqrt(2). If false, uses Uniform(0.8, 1.2). Defaults to true.\ncheck_args: Whether to validate input distributions. Defaults to true.\n\nReturns\n\nA DifferentialEvolutionSnookerSampler that can be used with setup_sampler_scheme or step or sample from AbstractMCMC.\n\nExample\n\nusing DifferentialEvolutionMetropolis, Distributions\n\n# Setup snooker update with custom gamma distribution\nsnooker_update = setup_snooker_update(γ = Uniform(0.1, 2.0))\n\nSee also setup_de_update, setup_subspace_sampling, setup_sampler_scheme.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.setup_snooker_update"},{"category":"function","location":"#DifferentialEvolutionMetropolis.setup_subspace_sampling","page":"DifferentialEvolutionMetropolis Documentation","text":"Set up a Subspace Sampling (DREAM-like) update step for MCMC sampling.\n\nCreates a sampler that updates only a random subset of parameters in each iteration, using multiple scaled difference vectors. The crossover probability determines which parameters to update and can be adapted during warm-up for improved efficiency.\n\nSee doi.org/10.1515/IJNSNS.2009.10.3.273 for more information.\n\nKeyword Arguments\n\nγ: Scaling factor for the difference vector sum. If nothing (default), uses the adaptive formula 2.38 / sqrt(2 * δ * d) where d is the number of updated dimensions. If a Real is provided, uses that fixed value throughout sampling.\ncr: Crossover probability for parameter selection. Can be a Real (fixed probability), nothing (adaptive using n_cr values), or a UnivariateDistribution. If cr is a DiscreteNonParametric then it those values can also be adapted. Defaults to nothing.\nn_cr: Number of crossover probabilities to adapt between when cr is nothing. Higher values allow more fine-tuned adaptation. Defaults to 3.\nδ: Number of difference vectors to sum. Can be an Integer (fixed) or a DiscreteUnivariateDistribution (random). Defaults to DiscreteUniform(1, 3).\nϵ: Distribution for small additive noise in the selected subspace. Defaults to Uniform(-1e-4, 1e-4).\ne: Distribution for multiplicative noise (1 + e) applied to the difference vector sum. Defaults to Normal(0.0, 1e-2).\ncheck_args: Whether to validate input distributions. Defaults to true.\n\nReturns\n\nA subspace sampler that can be used with setup_sampler_scheme or step or sample from AbstractMCMC.\n\nExample\n\nusing DifferentialEvolutionMetropolis, Distributions\n\n# Setup subspace sampling with custom crossover rate and delta\nsubspace_config = setup_subspace_sampling(cr = Beta(1, 2), δ = 2)\n\nSee also setup_de_update, setup_snooker_update, setup_sampler_scheme.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.setup_subspace_sampling"},{"category":"function","location":"#DifferentialEvolutionMetropolis.setup_hmc_update","page":"DifferentialEvolutionMetropolis Documentation","text":"setup_hmc_update(sampler; metric_strategy = ...)\n\nWrap an AdvancedHMC sampler (NUTS, HMC, HMCDA, or a hand-built HMCSampler) so it can participate as one update inside a setup_sampler_scheme composite, running the same HMC kernel on each chain in turn.\n\nThe implementation lives in the AdvancedHMCExt package extension; calling this requires AdvancedHMC.jl to be loaded.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.setup_hmc_update"},{"category":"function","location":"#DifferentialEvolutionMetropolis.memory_metric","page":"DifferentialEvolutionMetropolis Documentation","text":"memory_metric(; shrinkage = 0.0, every = 100)\n\nwarning: Experimental\nArchive-based metric strategies (memory_metric, cluster_pooled_metric, per_cluster_metric) are experimental; their behaviour and interface may change in future releases.\n\nMetric strategy for setup_hmc_update that estimates the HMC mass matrix from the sampler's memory archive rather than from AdvancedHMC's windowed adaptor. The estimate is a batch covariance over the history of sample positions, recomputed wholesale every every HMC steps so it keeps tracking the growing archive throughout both warm-up and sampling (recomputing the mass matrix between trajectories is valid: each HMC step is correct for whatever metric it uses). The step size adapts through dual averaging during warm-up and is then fixed.\n\nPass it through setup_hmc_update:\n\nsetup_hmc_update(NUTS(0.8); n_dims = d, metric_strategy = memory_metric())\n\nThis strategy requires the scheme to run with memory and is incompatible with parallel tempering — the memory archive interleaves hot-chain positions there, so the covariance would mix temperatures. Both cases raise an error on the first warmup step. Annealing is supported: when well-specified its chains all cool, so the archive trends to a clean cold-target sample.\n\nKeyword arguments:\n\nshrinkage: λ ∈ [0, 1]. Blends the estimate toward an isotropic target (v̄·I) to bound the metric's condition number; 0 keeps the raw sample covariance. A small ridge/floor is always applied so the metric stays positive-definite.\nevery: recompute the covariance every every HMC steps (≥ 1), in both warm-up and sampling. More frequent recomputes track the archive more closely at the cost of more covariance passes.\n\nThe estimate matches the metric the update was built with: a diagonal metric (the NUTS/HMC/HMCDA default) gets per-coordinate variances, while a dense metric (NUTS(0.8; metric = :dense)) gets the full covariance, capturing correlations between parameters. Requires AdvancedHMC.jl to be loaded.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.memory_metric"},{"category":"function","location":"#DifferentialEvolutionMetropolis.cluster_pooled_metric","page":"DifferentialEvolutionMetropolis Documentation","text":"cluster_pooled_metric(; shrinkage = 0.0, every = 100, kmax = 10)\n\nwarning: Experimental\nArchive-based metric strategies (memory_metric, cluster_pooled_metric, per_cluster_metric) are experimental; their behaviour and interface may change in future releases.\n\nMetric strategy for setup_hmc_update that estimates the HMC mass matrix from the within-cluster-pooled covariance of the memory archive. It is a multimodal-robust refinement of memory_metric: the archive is clustered, each point is centred on its cluster mean, and the centred deviations are pooled into a single covariance. Removing the per-cluster means strips the between-mode spread that otherwise inflates the raw memory covariance for separated modes, while still producing one position-independent metric shared by every chain.\n\nPass it through setup_hmc_update:\n\nsetup_hmc_update(NUTS(0.8); n_dims = d, metric_strategy = cluster_pooled_metric())\n\nThe adjustment is gated so it costs nothing on unimodal targets: a cheap per-axis multimodality test runs first, and clustering only happens when an axis looks multimodal and the between-mode variance is a material fraction of the total. When neither fires the metric is exactly memory_metric's raw memory covariance, so this strategy weakly dominates it — equal when unimodal, better when modes separate.\n\nIt carries the same requirements as memory_metric: the scheme must run with memory and is incompatible with parallel tempering (pure annealing is fine). Both are checked on the first warmup step.\n\nKeyword arguments:\n\nshrinkage: λ ∈ [0, 1], blended into the pooled estimate exactly as in memory_metric.\nevery: recompute the metric (and re-cluster) every every HMC steps (≥ 1), in both warm-up and sampling, exactly as in memory_metric.\nkmax: upper bound on the number of clusters (≥ 1). The cluster count is chosen up to this cap by growing it while each added cluster explains a worthwhile share of the variance, so a too-large kmax only costs a little extra compute.\n\nThe estimate matches the metric the update was built with (diagonal or dense), as for memory_metric. Requires AdvancedHMC.jl to be loaded.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.cluster_pooled_metric"},{"category":"function","location":"#DifferentialEvolutionMetropolis.per_cluster_metric","page":"DifferentialEvolutionMetropolis Documentation","text":"per_cluster_metric(; shrinkage = 0.0, every = 100, kmax = 10)\n\nwarning: Experimental\nArchive-based metric strategies (memory_metric, cluster_pooled_metric, per_cluster_metric) are experimental; their behaviour and interface may change in future releases.\n\nMetric strategy for setup_hmc_update that gives each posterior mode its own HMC mass matrix. It is the heteroscedastic refinement of cluster_pooled_metric: where the pooled strategy clusters the memory archive and collapses the per-cluster covariances into one shared metric (correct only when the modes share a shape), this one keeps the cluster covariances separate — one metric per cluster — and routes each chain to its current mode's metric. It is the right choice for separated modes of genuinely different shape, which a single pooled metric cannot fit at once.\n\nPass it through setup_hmc_update:\n\nsetup_hmc_update(NUTS(0.8); n_dims = d, metric_strategy = per_cluster_metric())\n\nThe K cluster metrics (and their centres) are recomputed only on the recompute cadence (every every steps), but each chain is relabelled to its nearest mode's metric before every HMC step, off its current position — so a chain that crosses into another mode immediately runs under that mode's shape. For separated modes the nearest centre is constant throughout each basin, so the per-mode metric is the optimal preconditioner and the choice does not depend on the step's own trajectory; for close modes whose trajectories can bridge clusters, choosing the metric from the step's own starting position introduces a small boundary bias in exchange for always matching the chain's current mode. Reparameterising the modes to a common shape (so cluster_pooled_metric suffices) is preferable when it is available.\n\nIt carries the same requirements as memory_metric (memory = true, no parallel tempering; pure annealing is fine), checked on the first warmup step. The adjustment is gated like cluster_pooled_metric: when the archive looks unimodal it reduces to one cluster and the metric is exactly memory_metric's.\n\nKeyword arguments shrinkage, every, and kmax behave exactly as in cluster_pooled_metric. The estimate matches the metric the update was built with (diagonal or dense). Requires AdvancedHMC.jl to be loaded.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.per_cluster_metric"},{"category":"function","location":"#AbstractMCMC.step","page":"DifferentialEvolutionMetropolis Documentation","text":"step(rng, model_wrapper, sampler, state; parallel=false, update_memory=true, kwargs...)\n\nPerform a single MCMC step using differential evolution sampling.\n\nThis is the core sampling function that proposes new states for all chains and accepts or rejects them according to the Metropolis criterion. For adaptive samplers, the function automatically fixes adaptive parameters before sampling.\n\nArguments\n\nrng: Random number generator\nmodel_wrapper: LogDensityModel containing the target log-density function\nsampler: Differential evolution sampler (any AbstractDifferentialEvolutionSampler)\nstate: Current state of all chains\n\nKeyword Arguments\n\nparallel: Whether to run chains in parallel using threading. Defaults to false. Advisable for slow models.\nupdate_memory: Whether to update the memory with new positions (for memory-based samplers). Defaults to true. Over writes memory options given at initialization.\nkwargs...: Additional keyword arguments passed to update functions (see https://turinglang.org/AbstractMCMC.jl/stable/api/#Common-keyword-arguments)\n\nReturns\n\nsample: DifferentialEvolutionSample containing new positions and log-densities\nnew_state: Updated state for the next iteration\n\nExample\n\nsample, new_state = step(rng, model, sampler, state; parallel=true)\n\nSee also step_warmup, sample from AbstractMCMC.\n\n\n\n\n\nstep(rng, model_wrapper, sampler, state::DifferentialEvolutionState; update_memory=true, kwargs...)\n\nEvaluate an adaptive step. Delegates to the inner step after fixing the sampler state, correctly forwarding the update_memory flag, and updating the outer state with the resulting memory and temperature ladder without duplicating memory writes or temperature ladder advances.\n\n\n\n\n\nstep(rng, model_wrapper, sampler; n_chains, memory=true, N₀, adapt=true, initial_position=nothing, parallel=false, kwargs...)\n\nInitialize differential evolution sampling by setting up chains and computing initial state.\n\nThis function serves as the entry point for differential evolution MCMC sampling. It handles chain initialization, memory setup for memory-based samplers, adaptive state initialization, and returns the initial sample and state that can be used with AbstractMCMC.sample.\n\nArguments\n\nrng: Random number generator\nmodel_wrapper: LogDensityModel containing the target log-density function\nsampler: Differential evolution sampler to use\n\nKeyword Arguments\n\nGeneric DE Arguments\n\nn_chains: Number of parallel chains. Defaults to max(2 * dimension, 3) for adequate mixing.\nadapt: Whether to enable adaptive behavior during warm-up (if the sampler supports it). Defaults to true.\ninitial_position: Starting positions for chains. Can be nothing (random initialization), or a vector of parameter vectors. If the provided vector is smaller than n_chains + n_hot_chains, it will be expanded; if larger and memory=true, excess positions become initial memory. Defaults to nothing.\nparallel: Whether to evaluate initial log-densities in parallel. Useful for expensive models. Defaults to false.\nn_preallocated_indices: This package provides fast sampling-without-replacement by pre-allocating indices, defaults to 3 (which the most asked for by the implemented samplers). Consider increasing it if you implement your own proposal that calls pick_chains with n_chains > 3.\nsilent: Suppress informational logging during initialization (e.g., initial position adjustments and   memory setup) when true. Defaults to false.\n\nMemory-based Sampling Arguments\n\nmemory: Whether to use memory-based sampling that stores past positions. Memory-based samplers can be more efficient for high-dimensional problems. Defaults to true.\nN₀: Initial memory size for memory-based samplers. Should be ≥ n_chains + n_hot_chains. Defaults to 2 * (n_chains + n_hot_chains).\nupdate_memory: Whether to update the memory with new positions (for memory-based samplers). Defaults to true. Overwrites memory options given at initialization, generally should only be of use if calling step directly.\nmemory_refill: Whether to refill memory when full instead of extending the memory, will replace from the start. Defaults to false.\nmemory_size: Maximum number of positions preallocated per chain in memory. The effective number stored positions is memory_size * (n_chains + n_hot_chains). Defaults to 1001 or 2*num_warmup if that is provided here or via sample. If memory_refill = true this is the maximum number stored before refilling, if  memory_refill = false once the memory is full, the array is extended by another memory_size worth of positions. Set with consideration of available RAM and expected run length.\nmemory_thin_interval: Thinning interval for memory updates. If > 0, only every memory_thin_interval-th position is stored in memory.\n\nParallel Tempering and Simulated Annealing Arguments\n\nn_hot_chains: Number of hot chains for parallel tempering. Defaults to 0 (no parallel tempering).\nmax_temp_pt: Maximum temperature for parallel tempering. Defaults to 2*sqrt(dimension).\nmax_temp_sa: Maximum temperature for simulated annealing. Defaults to max_temp_pt.\nα: Temperature ladder spacing parameter. Controls the geometric spacing between temperatures. Defaults to 1.0.\nannealing: Whether to use simulated annealing (temperature decreases over time). Defaults to false.\nannealing_steps: Number of annealing steps. Defaults to annealing ? num_warmup : 0.\ntemperature_ladder: Pre-defined temperature ladder as a vector of vectors. If provided, overrides automatic temperature ladder creation. Defaults to create_temperature_ladder(n_chains, n_hot_chains, α, max_temp_pt, max_temp_sa, annealing_steps).\n\nReturns\n\nsample: DifferentialEvolutionSample containing initial positions and log-densities\nstate: Initial state (DifferentialEvolutionState) ready for sampling\n\nExamples\n\nusing DifferentialEvolutionMetropolis, Random, Distributions\n\n# Setup\nrng = Random.default_rng()\nmodel_wrapper(θ) = logpdf(MvNormal([0.0, 0.0], I), θ)\nsampler = deMCzs()\n\n# Basic initialization with default settings\nsample, state = step(rng, model_wrapper, sampler)\n\n# Custom number of chains with memory disabled\nsample2, state2 = step(rng, model_wrapper, sampler; n_chains=10, memory=false)\n\n# With custom initial positions\ninit_pos = [randn(2) for _ in 1:8]\nsample3, state3 = step(rng, model_wrapper, sampler; initial_position=init_pos)\n\nNotes\n\nFor non-memory samplers, n_chains should typically be ≥ dimension for good mixing\nMemory-based samplers can work effectively with fewer chains than the problem dimension\nInitial log-densities are computed automatically for all starting positions\nWhen using parallel tempering (n_hot_chains > 0), only the cold chains (first n_chains) are returned in the sample, but all chains participate in the sampling process\nMemory-based samplers with parallel tempering will issue warnings since hot chains typically aren't necessary when using memory\n\nSee also sample from AbstractMCMC, deMC, deMCzs, DREAMz.\n\n\n\n\n\n","title":"AbstractMCMC.step"},{"category":"function","location":"#AbstractMCMC.step_warmup","page":"DifferentialEvolutionMetropolis Documentation","text":"step_warmup(rng, model_wrapper, sampler, state; parallel=false, kwargs...)\n\nPerform a single MCMC step during the warm-up (adaptive) phase.\n\nDuring warm-up, this function performs the same sampling as step but also updates adaptive parameters. For subspace samplers, it adapts crossover probabilities based on the effectiveness of different parameter subsets.\n\nArguments\n\nrng: Random number generator\nmodel_wrapper: LogDensityModel containing the target log-density function\nsampler: Adaptive differential evolution sampler\nstate: Current state including adaptive parameters\n\nKeyword Arguments\n\nupdate_memory: Whether to update the memory with new positions (for memory-based samplers). Defaults to true. Useful if memory has grown too large.\nparallel: Whether to run chains in parallel using threading. Defaults to false.\nkwargs...: Additional keyword arguments passed to update functions\n\nReturns\n\nsample: DifferentialEvolutionSample containing new positions and log-densities\nnew_state: Updated state with adapted parameters for the next iteration\n\nExample\n\nusing DifferentialEvolutionMetropolis, Random, Distributions\n\n# Setup for warmup step example\nrng = Random.default_rng()\nmodel_wrapper(θ) = logpdf(MvNormal([0.0, 0.0], I), θ)\nsampler = DREAMz()\n\n# Initialize state (this would typically be done by AbstractMCMC.sample)\n# sample, new_state = step_warmup(rng, model_wrapper, sampler, state; parallel=false)\n\nSee also step, fix_sampler.\n\n\n\n\n\nstep_warmup(rng, model_wrapper, sampler, state; kwargs...)\n\nPerform a single MCMC step during warm-up for composite samplers.\n\nFor composite samplers, this function randomly selects one of the component update methods, performs a warm-up step with that method, and updates the corresponding adaptive state while preserving other component states.\n\nArguments\n\nrng: Random number generator\nmodel_wrapper: LogDensityModel containing the target log-density function\nsampler: Composite differential evolution sampler\nstate: Current state with composite adaptive parameters\n\nKeyword Arguments\n\nupdate_memory: Whether to update the memory with new positions. Defaults to true. This is forwarded to the inner step, and the outer state's memory is updated without duplicating writes.\nkwargs...: Additional keyword arguments passed to component update functions\n\nReturns\n\nsample: DifferentialEvolutionSample containing new positions and log-densities\nnew_state: Updated state with adapted parameters for the selected component\n\nSee also step_warmup, setup_sampler_scheme.\n\n\n\n\n\n","title":"AbstractMCMC.step_warmup"},{"category":"function","location":"#DifferentialEvolutionMetropolis.fix_sampler","page":"DifferentialEvolutionMetropolis Documentation","text":"fix_sampler(sampler::AbstractDifferentialEvolutionSampler, adaptive_state::AbstractDifferentialEvolutionAdaptiveState)\n\nFix adaptive parameters of a sampler to their current adapted values.\n\nFor non-adaptive samplers, returns the sampler unchanged. For adaptive samplers, returns a new sampler with the adaptive parameters fixed to their current values in the adaptive_state.\n\nArguments\n\nsampler: The differential evolution sampler to fix\nadaptive_state: The adaptive state containing current parameter values\n\nReturns\n\nA sampler with fixed (non-adaptive) parameters\n\nExample\n\nusing DifferentialEvolutionMetropolis, Random, Distributions\n\n# This function is typically used after warmup/adaptation phase\n# fixed_sampler = fix_sampler(adaptive_sampler, state.adaptive_state)\n\nSee also fix_sampler_state.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.fix_sampler"},{"category":"function","location":"#DifferentialEvolutionMetropolis.fix_sampler_state","page":"DifferentialEvolutionMetropolis Documentation","text":"fix_sampler_state(sampler::AbstractDifferentialEvolutionSampler, state::DifferentialEvolutionState)\n\nFix adaptive sampler parameters and return a corresponding non-adaptive state.\n\nTakes an adaptive sampler and state, fixes the sampler's adaptive parameters to their current values, and returns both the fixed sampler and a simplified state without adaptive components.\n\nArguments\n\nsampler: The differential evolution sampler (potentially adaptive)\nstate: The current sampler state (DifferentialEvolutionState)\n\nReturns\n\nfixed_sampler: Sampler with adaptive parameters fixed to current values\nfixed_state: State without adaptive components\n\nExample\n\nusing DifferentialEvolutionMetropolis, Random, Distributions\n\n# This function is typically used after warmup/adaptation phase\n# fixed_sampler, fixed_state = fix_sampler_state(sampler, state)\n\nSee also fix_sampler.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.fix_sampler_state"},{"category":"function","location":"#DifferentialEvolutionMetropolis.r̂_stopping_criteria","page":"DifferentialEvolutionMetropolis Documentation","text":"r̂_stopping_criteria(rng, model, sampler, samples, state, iteration; kwargs...)\n\nStopping criterion based on the Gelman-Rubin diagnostic (R̂).\n\nSampling continues until the R̂ value for all parameters falls below maximum_R̂, indicating convergence across chains. This function is designed to be used as the N_or_isdone argument in AbstractMCMC.sample for adaptive stopping.\n\nThe diagnostic is computed on the last half of the collected samples to focus on the stationary portion of the chains.\n\nArguments\n\nrng: Random number generator (unused but required by AbstractMCMC interface)\nmodel: The model being sampled (unused but required by interface)\nsampler: The differential evolution sampler (unused but required by interface)\nsamples: Vector of collected samples from all chains\nstate: Current sampler state (unused but required by interface)\niteration: Current iteration number\n\nKeyword Arguments\n\ncheck_every: Frequency (in iterations) for checking R̂ values. Defaults to 1000.\nmaximum_R̂: Maximum acceptable R̂ value for convergence. Defaults to 1.2.\nmaximum_iterations: Maximum number of iterations before forced stopping. Defaults to 100000.\nminimum_iterations: Minimum iterations before convergence checking begins. Defaults to 0.\n\nReturns\n\ntrue if sampling should stop (converged or maximum iterations reached)\nfalse if sampling should continue\n\nExample\n\nusing DifferentialEvolutionMetropolis, AbstractMCMC, Random, Distributions\n\n# Create a simple model\nmodel_wrapper(θ) = logpdf(MvNormal([0.0, 0.0], I), θ)\n\n# Setup sampler\nsampler = deMCzs()\n\n# Use with adaptive stopping criterion\nrng = Random.default_rng()\nchains = sample(rng, model_wrapper, sampler, r̂_stopping_criteria;\n               n_chains=4, check_every=500, maximum_R̂=1.1)\n\nSee also MCMCDiagnosticTools.rhat, deMCzs, DREAMz.\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.r̂_stopping_criteria"},{"category":"type","location":"#DifferentialEvolutionMetropolis.DifferentialEvolutionOutput","page":"DifferentialEvolutionMetropolis Documentation","text":"DifferentialEvolutionOutput{T <: Real}\n\nContainer for differential evolution MCMC sampling results.\n\nFields\n\nsamples::Array{T, 3}: Three-dimensional array of parameter samples with dimensions (iterations, chains, parameters). Each sample represents a point in parameter space from the MCMC chain.\nld::Matrix{T}: Two-dimensional matrix of log-density values with dimensions (iterations, chains). Contains the log-probability density evaluated at each corresponding sample point.\n\nType Parameters\n\nT <: Real: Numeric type for the samples and log-density values (typically Float64).\n\nExamples\n\n# Access samples from the output\noutput = sample(model, sampler, n_samples)\nparameter_samples = output.samples  # Shape: (n_samples, n_chains, n_params)\nlog_densities = output.ld          # Shape: (n_samples, n_chains)\n\n# Extract samples for a specific chain\nchain_1_samples = output.samples[:, 1, :]  # All samples from chain 1\n\n\n\n\n\n","title":"DifferentialEvolutionMetropolis.DifferentialEvolutionOutput"},{"category":"section","location":"tutorial/#Sampling-from-multimodal-distributions","page":"Sampling from multimodal distributions","text":"Let say you have a multimodal distribution, for example a mixture of two Gaussians. DifferentialEvolutionMetropolis implements differential evolution MCMC samplers (including deMC-zs and DREAMz) that are designed to sample from such distributions efficiently. Roughly these samplers work by generating new proposals based on many separate chains (or a history of sampled chains). In theory this allows the sampler to easily jump between modes of the distribution.","title":"Sampling from multimodal distributions"},{"category":"section","location":"tutorial/#Multimodal-Distributions","page":"Sampling from multimodal distributions","text":"First we need to implement a multimodal problem. We will assume that our data is generated by a mixture of two 2D-Gaussians with means 5 and -5 and a 65% bias towards the positive value. Then the distributions of the means will be multimodal and highly correlated with the probability. We can easily implement this using the Distributions package, which underpins most of the functionality in DifferentialEvolutionMetropolis.\n\nusing Distributions, Random\n\nmean = 5.0\nstd = 2.0\np = 0.65\n\ndist = MixtureModel([\n    Normal(mean, std),\n    Normal(-mean,  std)\n], [p, 1 - p]);\n\n#generate our data\nRandom.seed!(1234)\ndata = rand(dist, 1000)\n\n# setup our log density\nstruct MixtureNormal\n    #data\n    data::Vector{Float64}\n    #prior parameters\n    prior_μ_σ::Float64\n    prior_σ_σ::Float64\nend\n\n#initialize the model\nmodel = MixtureNormal(data, 10.0, 10.0)\n\n# implement the log density function\nfunction (problem::MixtureNormal)(θ)\n    (; μ, p, σ) = θ\n    ld = \n        #priors\n        logpdf(Normal(0, problem.prior_σ_σ), σ) + #half-normal because σ > 0\n        logpdf(Uniform(0, 1), p) #prior on mixture weight\n    μ_prior = Normal(0, problem.prior_μ_σ)\n    for μ_ in μ\n        ld += logpdf(μ_prior, μ_)\n    end\n    #likelihood\n    ll_model = MixtureModel(\n        [\n            Normal(μ[1], σ),\n            Normal(μ[2], σ)\n        ], [p, 1 - p]\n    )\n    for i in axes(problem.data, 1)\n        ld += logpdf(ll_model, problem.data[i])\n    end\n    return ld\nend\n\n# to visualize our data\nusing Plots\nplot(histogram(data, bins = 30, xlabel = \"Value\", ylabel = \"Frequency\", title = \"Histogram of Generated Data\"))\n\nWe can also transform our log density function, so we can provide real-valued inputs. This is much easier to work with.\n\nusing TransformedLogDensities, TransformVariables\n\ntransformation = as((μ = as(Array, 2), p = as𝕀, σ = asℝ₊))\ntransformed_ld = TransformedLogDensity(transformation, model)","title":"Multimodal Distributions"},{"category":"section","location":"tutorial/#Sampling-with-DifferentialEvolutionMetropolis","page":"Sampling from multimodal distributions","text":"Now let's use DifferentialEvolutionMetropolis to sample from this multimodal distribution. Here we use the DREAMz sampler, which is well-suited for exploring complex, multimodal spaces. We increase the number of chains to allow the sampler to explore the distribution more effectively.\n\nusing DifferentialEvolutionMetropolis, AbstractMCMC\n\nmodel = AbstractMCMC.LogDensityModel(transformed_ld)\n\n# Sample using DREAMz with adaptive stopping based on convergence\ndreamz = DREAMz(model, 10000; n_chains = 6, progress = true);\n\nOther implementations of the differential evolution MCMC algorithm are available in DifferentialEvolutionMetropolis.jl, such as deMC and deMCzs, which can be used similarly.","title":"Sampling with DifferentialEvolutionMetropolis"},{"category":"section","location":"tutorial/#Custom-Scheme","page":"Sampling from multimodal distributions","text":"DREAMz can be further customized. For example, we could include snooker updates alongside the DREAMz-like subspace sampling.\n\nYou can also modify aspects of the implemented sampling, for example tell DREAMz to use non-memory-based sampling with DREAMz(...; memory = false), or you can define your own sampler scheme for more control over the sampling process.\n\nThis time we will use MCMCChains.jl to the handle the output.\n\nusing MCMCChains\n\n# Create a custom sampler scheme combining different update types\ncustom_sampler = setup_sampler_scheme(\n    setup_subspace_sampling(), # a DREAM-like sampler that uses subspace sampling\n    setup_snooker_update(deterministic_γ = false), # a snooker update for better exploration\n    setup_de_update(); # standard DE update\n    w = [0.6, 0.2, 0.2] # weights for each update type\n);\n\n# Sample using AbstractMCMC.sample with custom stopping criteria\ncustom_results = sample(\n    model,\n    custom_sampler,\n    r̂_stopping_criteria;\n    check_every = 10000,\n    maximum_R̂ = 1.05,\n    n_chains = 4,\n    N₀ = 100, # want good exploration of the space\n    memory = true,\n    parallel = true,\n    annealing = true,\n    num_warmup = 10000,\n    memory_size = 5000,\n    memory_refill = true,\n    chain_type = MCMCChains.Chains\n);\n\nYou can also define your own samplers for more specialized use cases by extending the abstract types.","title":"Custom Scheme"},{"category":"section","location":"tutorial/#Interpreting-Results","page":"Sampling from multimodal distributions","text":"After running the sampler, you will have a collection of samples from the target distribution. These samples can be used to estimate summary statistics, credible intervals, and to assess the quality of your sampling.","title":"Interpreting Results"},{"category":"section","location":"tutorial/#Assessing-Sampler-Performance:-ESS-and-R-hat","page":"Sampling from multimodal distributions","text":"To evaluate how well your sampler is performing, you can compute the effective sample size (ESS) and the R-hat diagnostic. These metrics help you determine if your chains have mixed well and if your estimates are reliable.\n\nEffective Sample Size (ESS): This measures the number of independent samples your chains are equivalent to. Higher ESS values indicate more reliable estimates.\nR-hat Diagnostic: Also known as the Gelman-Rubin statistic, R-hat compares the variance within each chain to the variance between chains. Values close to 1 suggest good mixing and convergence; values much greater than 1 indicate potential problems.\n\nBelow is an example of how to compute these diagnostics for the DifferentialEvolutionMetropolis samplers using MCMCDiagnosticTools:\n\nusing Statistics, MCMCDiagnosticTools\n\n# Compute diagnostics for DREAMz results\ness_val = ess(dreamz.samples) ./ size(dreamz.samples, 1)\nrhat_val = maximum(rhat(dreamz.samples))\n\nprintln(\"DREAMz diagnostics:\")\nprintln(\"  ESS per iteration: $ess_val\")\nprintln(\"  R-hat: $rhat_val\")\n\n# example trace plot\nplot(dreamz.samples[:, :, 1], xlabel = \"Iteration\", ylabel = \"Value\", title = \"Trace Plot for Mean 1 (DREAMz)\")","title":"Assessing Sampler Performance: ESS and R-hat"},{"category":"section","location":"tutorial/#Summarizing-Posterior-Samples","page":"Sampling from multimodal distributions","text":"Once you have confirmed good mixing and convergence, you can summarize your posterior samples. For each parameter, you may want to compute the median and a credible interval (such as the 90% interval):\n\n#need to transform the samples to original space, there is currently no nice way to do this\ncustom_results = cat([dreamz.samples[:, c, :] for c in axes(dreamz.samples, 2)]...; dims = 1)\nfor it in axes(custom_results, 1)\n    transformed_values = transform(transformation, Array(custom_results[it, :]))\n    custom_results[it, :] .= [transformed_values.μ..., transformed_values.p, transformed_values.σ]\nend\n\n#median for variance\nmed = median(custom_results[:, 4])\nq05, q95 = quantile(custom_results[:, 4], [0.05, 0.95])\nprintln(\"Standard deviation: posterior median = $med, 90% CI = ($q05, $q95)\")\nprintln(\"True standard deviation: $std\")\n\n#not so sensible for p or means\nmed = median(custom_results[:, 1])\nq05, q95 = quantile(custom_results[:, 1], [0.05, 0.95])\nprintln(\"Mean 1: posterior median = $med, 90% CI = ($q05, $q95)\")\nprintln(\"True mean: $(mean)\")\n\n#visualize the posteriors, expect multimodal distributions\nplot(histogram(custom_results[:, 1], bins = 30, xlabel = \"Mean 1\", ylabel = \"Frequency\"), histogram(custom_results[:, 2], bins = 30, xlabel = \"Mean 2\", ylabel = \"Frequency\"), histogram(custom_results[:, 3], bins = 30, xlabel = \"Probability\", ylabel = \"Frequency\"), layout = (3, 1))\n\n#covariance\nplot(scatter(custom_results[:, 1], custom_results[:, 3], xlabel = \"Mean 1\", ylabel = \"Probability of Normal 1\"), scatter(custom_results[:, 1], custom_results[:, 2], xlabel = \"Mean 1\", ylabel = \"Mean 2\"), layout = (2, 1))\n\nFor more details, see the DifferentialEvolutionMetropolis Documentation and Customizing your sampler.","title":"Summarizing Posterior Samples"}]
}
